# 音效知识库维护规则

本仓库是网站、视频学习方法和 `sfx-knowledge` Skill 的共同事实来源。开始工作前先读 `skills/sfx-knowledge/SKILL.md`，按需读取其 `references/`。

## 每条视频的完成标准

1. 必须取得可检查的完整视频画面，下载目标上限保持 1080p。字幕只用于搜索和时间定位，不能单独作为分析证据；只有字幕时先解决视频获取，仍失败则跳过。
2. 从头到尾检查时间线，记录声音设计目标、素材角色、分层、处理顺序、参数方向、可见数值、路由、自动化、中间件和设计理由。明确区分画面确认值与分析推断，禁止编造参数。
3. 每个视频在 `index.html` 中保留一个独立记录和独立详情页，不把多个视频合并成一篇。标题使用中文，保留 `videoId`、来源和日期。
4. 步骤应细到可以复刻。重点覆盖素材如何变形、每颗插件解决什么问题、前后级如何互相影响、参数调节先后顺序以及失败时如何修正。
5. 每个关键步骤配清晰截图。预览使用 `assets/shots/preview/`，点击放大使用 `assets/shots/full/`；插件处理点下面只放核对过型号的官方插件图片，尺寸保持紧凑。
6. 参数变化、路由切换或前后对比仅在静态图表达不清时制作清晰 MP4 流程动图，放入 `assets/motions/`，并保留轻量 poster。不要用体积更大的 GIF。
7. 页面需要兼顾手机加载速度：首屏和列表懒加载预览图，高清图只在放大时加载，视频动图点击后播放，避免一次请求所有资源。
8. 每条视频完成后同时更新 `skills/sfx-knowledge/references/video-learnings.md`；再运行 `node tools/export-site-memory.cjs`，保证网站全部记录都可从 Skill 检索。
9. 运行 `node tools/verify-portable-kit.cjs`，检查 100% 视频记忆覆盖、资源引用、脚本语法和敏感信息边界，然后再提交和推送。

## 分析结构

每个网站记录至少包含：

- `summary`：教程真正解决的问题。
- `coreIdeas`：可迁移的设计原则。
- `steps`：顺序、动作、细节、参数证据和图片键。
- `plugins`：插件名称、用途、设置及调参逻辑。
- `materials`：素材及每层职责。
- `chainFocus`：完整效果链的因果关系。
- `parameterLogic`：先调什么、听什么、何时停止。
- `effectUses`：可选的结构化效果器实际用法；记录处理对象、链路位置、用途、参数、结果、上下游关系、限制、截图键和证据标签。

网站是完整视频干货档案，不生成练习、作业、打卡、难度或预计学习时间。字幕只用于定位；每条效果器结论必须标明画面确认、作者口述、音频可辨、分析推断或视频未展示。

## 数据与安全

- 允许提交：网站源文件、压缩后的教学截图/动图、官方插件参考图、工具代码、Skill 和 Markdown 记忆。
- 禁止提交：原始 YouTube 视频、完整音轨、Cookie、登录态、API Key、`.env`、`.venv`、`.work`、下载临时文件和本机绝对路径配置。
- 不再使用旧的 API 驱动帧分析流水线。视觉理解由当前 Codex 直接检查本机 1080p 帧完成。
- 更新 Skill 时以仓库内 `skills/sfx-knowledge/` 为准；完成后用 `tools/install-sfx-skill.ps1 -Force` 同步到当前电脑。
